name: universal_pet_health_record
publish_to: none
description: Aplicativo do tutor para o Universal Pet Health Record.
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.6
  file_picker: ^10.3.2
  flutter_secure_storage: ^9.2.4
  http: ^1.5.0
  intl: ^0.20.2
  mime: ^2.0.0
  qr_flutter: ^4.1.0
  url_launcher: ^6.3.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
