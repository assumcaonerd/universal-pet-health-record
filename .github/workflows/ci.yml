name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies (placeholder)
        run: |
          echo "Placeholder for dependency installation"
          # npm ci (quando package.json existir)

      - name: Run tests (placeholder)
        run: |
          echo "Placeholder for tests"
          # npm test

      - name: Validate JSON Schemas
        run: |
          echo "Validating schemas..."
          # Adicionar validação de JSON Schema aqui quando ferramentas estiverem disponíveis
